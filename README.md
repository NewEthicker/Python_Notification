# Custom_Python_Notification
It's a simple program that uses the Plyer library to create custom notifications.

# Before running the code install these libraries : #
             # pip install plyer #
         # pip install notification #
